# Typescript React simple todo app

This is a simple (*and crude*) todo app for testing using `Typescript` with `React` as well as learning about testing `React` components using `Jest` and the `React Testing Library`.